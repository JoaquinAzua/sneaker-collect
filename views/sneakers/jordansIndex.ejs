<%- include('../partials/header') %>

<style>
  td {
    text-align: center;
  }
</style>

<div class="jordans">
  <table style="font-size: 25px" class="table table-borderless">
    <thead>
      <tr>
        <br />
        <h1 style="text-align: center">JORDANS</h1>
        <th></th>
        <th></th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% jordans.forEach(function(j) { %>
      <tr>
        <td>
          <img
            style="height: 60px"
            class="rounded float-start"
            src="<%= j.thumbnail %>"
          />
        </td>
        <td><%= j.shoeName %><br /><%= j.releaseDate %></td>
        <td>
          <a
            class="btn btn-outline-dark"
            href="/owned/<%= j.styleID %>"
            style="font-size: 15px"
            >Add to Owned Collection</a
          >
        </td>
        <td>
          <a
            class="btn btn-outline-dark"
            href="/watchlist/<%= j.styleID %>"
            style="font-size: 15px"
            >Add to Wishlist</a
          >
        </td>
      </tr>
      <% }); %>
    </tbody>
  </table>
</div>


<%- include('../partials/footer') %>